{
  "name": "max-height",
  "version": "0.1.0",
  "private": true,
  "description": "Max Height — interactive AI character web experience",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --build",
    "validate": "npm run lint && npm run format:check && npm run typecheck && npm run build && npm run test"
  },
  "devDependencies": {
    "@eslint/js": "^9.27.0",
    "eslint": "^9.27.0",
    "prettier": "^3.5.0",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.33.0"
  },
  "overrides": {
    "zod": "^4.3.6"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
