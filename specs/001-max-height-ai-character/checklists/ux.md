# UX / Visual Presentation Checklist: Max Height AI Character

**Purpose**: Validate that UX and visual presentation requirements are complete, clear, consistent, and measurable — covering the CRT framing, avatar, broadcast mode, TV interaction model, glitch effects, and mobile visual parity.
**Created**: 2026-04-22
**Feature**: [spec.md](../spec.md)

## Requirement Completeness

- [x] CHK001 — Are layout dimensions and positioning specified for the CRT television frame (bezel size, aspect ratio, viewport proportions)? [Completeness, Spec §FR-001, §Visual Presentation] **Intentionally unspecified.** The spec defines constraints (CRT frame, bezel visible, head-and-shoulders inside) not pixel-perfect layouts. Exact dimensions are visual design decisions appropriate for implementation. The constraint-based approach is correct for this spec.
- [x] CHK002 — Are the visual specifications for the "Turn on the TV" knob defined (size, position on the CRT frame, affordance cues, animation on interaction)? [Completeness, Spec §FR-001] **Intentionally unspecified.** FR-001 defines the interaction (knob as single entry gesture). Visual design of the knob is an implementation detail. The spec correctly constrains behavior, not appearance.
- [x] CHK003 — Are requirements for the wireframe/vector backdrop defined with enough detail to implement (grid density, rotation speed, color palette values, animation behavior)? [Completeness, Spec §Visual Presentation] **Addressed with guidance.** Spec: "rotating or scrolling polygonal/grid pattern, period-appropriate palette (e.g., magenta/cyan on black)." R3F handles rendering. Exact parameters are creative decisions within the "1980s computer-generated" constraint.
- [x] CHK004 — Are the 2D placeholder avatar visual specifications documented (SVG dimensions, head-and-shoulders framing ratio, placement within CRT frame)? [Completeness, Spec §FR-006a] **Addressed at constraint level.** FR-006a: binary mouth state. Spec: "head and shoulders only." Plan: "SVG with binary mouth state." Exact dimensions depend on the artwork and CRT frame proportions.
- [x] CHK005 — Are requirements for the binary mouth-state animation specified (open/closed frame timing, sync method to audio playback, visual design of each state)? [Completeness, Spec §FR-006a] **Addressed.** FR-006a: "binary mouth state (open/closed) synced to audio playback to create a 'talking head' illusion." Sync method is specified (to audio playback). Frame timing and visual design are implementation details.
- [x] CHK006 — Are text display requirements for Max's response defined (font, size, positioning within the CRT frame, text overflow/truncation behavior, character-by-character vs. block reveal)? [Completeness, Spec §FR-026] **Intentionally unspecified.** FR-026 defines behavior (broadcast mode, current response only, no scrollable history). Visual styling (font, size, overflow) is CSS implementation detail within the CRT aesthetic.
- [x] CHK007 — Are requirements for the text input area and mic button layout specified (positioning relative to TV frame, sizing, spacing, always-visible constraint)? [Completeness, Spec §FR-026] **Addressed at constraint level.** FR-026: "text input area and mic button MUST be always visible below the TV frame." Positioning (below TV) and visibility (always) specified. Sizing/spacing are implementation details.
- [x] CHK008 — Are the "ON AIR" indicator visual requirements defined (size, color, position, animation)? [Completeness, Spec §FR-008] **Addressed at constraint level.** FR-008: "visible 'ON AIR' indicator MUST display while the mic is held." Functional requirement clear. Visual design is implementation detail within the broadcast/CRT aesthetic.
- [x] CHK009 — Are loading/buffering UX requirements for cold-start defined beyond "in-character"? (Visual treatment, animation, positioning within the CRT frame) [Completeness, Spec §FR-011] **Addressed.** FR-011: "in-character 'buffering' UX covering the wait." Bible §5 archetype 8 ("— b-b-buffering — there we are") and bible §3.5 AI jokes ("Pardon my buffer") provide character templates. Visual treatment within the CRT frame is implementation detail.
- [x] CHK010 — Are requirements for the in-character error states defined visually ("signal lost", "Max is taking a break", budget breach)? [Completeness, Spec §FR-014, §FR-015] **Addressed at constraint level.** FR-014: in-character error state, never white screen. FR-015: "Max is taking a break." The in-character requirement constrains the copy; visual presentation within the CRT frame follows the same aesthetic as all Max responses.
- [x] CHK011 — Are the "Forget me" and "Export" control placements and visual treatments specified? [Gap, Spec §FR-017, §FR-018] **Intentionally unspecified.** FR-017/FR-018 define the actions (wipe, export JSON). Placement and styling are UI implementation details. These are utility controls, not core experience elements.

## Requirement Clarity

- [x] CHK012 — Is "reduced intensity" for mobile glitch effects quantified with specific parameters (e.g., percentage reduction, which effects are reduced vs. removed)? [Clarity, Spec §Visual Presentation, US2 §2] **Intentionally qualitative.** Spec: "reduced intensity on mobile." US2 §2: "reduced by default compared to desktop." Exact reduction percentages are a performance tuning detail — the implementer measures fps impact and adjusts. The constraint is: reduced, not removed. Effects still run on mobile, just lighter.
- [x] CHK013 — Is "constant low-level glitching" defined with measurable parameters (frequency of frame skips, intensity of scanline artifacts, trigger conditions for "abrupt backdrop changes")? [Clarity, Spec §Visual Presentation] **Intentionally qualitative.** Spec lists effect types (frame skips, image tears, scanline artifacts, backdrop changes). These are shader/animation parameters tuned during implementation. The constraint is: continuous at low intensity, never "off." Exact parameters are creative + performance decisions.
- [x] CHK014 — Is "period-appropriate palette (e.g., magenta/cyan on black)" specified with exact color values, or is this intentionally left to implementation? [Clarity, Spec §Visual Presentation] **Intentionally left to implementation.** The spec provides direction ("period-appropriate") with examples ("magenta/cyan on black"). Exact hex values are visual design decisions. This is correct — the spec constrains the aesthetic era, not the exact colors.
- [x] CHK015 — Is "head and shoulders only" framing defined with specific crop ratios or reference imagery for the 2D avatar? [Clarity, Spec §Visual Presentation] **Addressed as concept.** The framing constraint is clear: head and shoulders, inside CRT, never full body. Exact crop ratios depend on the artwork. The anti-goals (no full-body, no chat bubble, no floating head) provide negative constraints.
- [x] CHK016 — Is "broadcast mode" display behavior fully specified — specifically, how the visitor's last input is shown "briefly" (duration? animation? positioning?) before being replaced? [Clarity, Spec §FR-026] **Addressed.** FR-026: "MAY be shown briefly... replaced when Max's next response arrives." The duration is naturally bounded by Max's response time (1.5s P95 per FR-004). The "MAY" makes this optional. No additional timing spec needed.
- [x] CHK017 — Is the "in-character 'buffering' UX" for cold start defined with specific visual/textual treatment, or just the requirement that it be in-character? [Ambiguity, Spec §FR-011] **Addressed.** Same as CHK009. FR-011 + bible provide character templates. The visual treatment (within CRT frame, matching the glitch aesthetic) follows from the overall visual constraints. Exact implementation is a design decision.

## Requirement Consistency

- [x] CHK018 — Are the visual framing requirements consistent between the MVP 2D avatar and the V1 3D avatar upgrade path? Does the spec confirm the CRT/bezel/backdrop framing stays the same? [Consistency, Spec §Visual Presentation, §FR-006a vs §FR-006] **Addressed.** Spec explicitly states: "The 3D upgrade in V1 is a fidelity improvement, not a re-conception." The CRT/bezel/wireframe backdrop framing persists from MVP to V1.
- [x] CHK019 — Are glitch effect requirements consistent between the Visual Presentation section and the performance requirement for 60fps CRT effects on desktop? [Consistency, Spec §Visual Presentation vs §SC-004] **Addressed.** No conflict. SC-004 requires 60fps for CRT effects on desktop. Visual Presentation describes what those effects are (glitching, scanlines, wireframe). The effects must run AT 60fps — R3F with GPU shaders can achieve this. Performance is a quality gate, not a constraint on which effects are allowed.
- [x] CHK020 — Are the anti-goal constraints (no full-body, no chat-bubble, no free-floating head) referenced in both the Visual Presentation section and any relevant acceptance scenarios? [Consistency, Spec §Visual Presentation] **Addressed.** Anti-goals are stated in Visual Presentation. They're design review criteria, not functional test assertions. Acceptance scenarios correctly test positive behavior (greeting plays, text appears, personality quality). Anti-goals are enforced via code review, not automated tests.

## Scenario Coverage

- [ ] CHK021 — Are requirements defined for the visual transition from "TV off" to "TV on" state (animation, timing, visual treatment of the CRT powering up)? [Coverage, Gap] **⚠️ Minor gap — UX polish opportunity.** The interaction is defined (FR-001: click knob → greeting begins) but no CRT power-up animation is specified. A brief CRT warm-up visual (static → signal → Max appears) would enhance the first impression. **Recommendation:** Add a note to spec §Visual Presentation: "The TV-on gesture SHOULD trigger a brief CRT power-up transition (≤1 second) before the greeting begins."
- [ ] CHK022 — Are requirements specified for what the TV/CRT looks like in its "off" state before the visitor clicks the knob? [Coverage, Gap] **⚠️ Minor gap — UX polish opportunity.** The landing state isn't specified. A powered-off CRT (dark screen, visible bezel, knob affordance) would set the mood before interaction. **Recommendation:** Add a note: "Before the visitor interacts, the page displays the CRT frame in a powered-off state with the knob visible as the primary affordance."
- [x] CHK023 — Are visual requirements for the interruption flow defined — what happens visually when the visitor interrupts Max mid-response (partial text behavior, avatar state, transition)? [Coverage, Spec §FR-027] **Addressed.** FR-027: "Max's audio MUST stop immediately... partial text remains visible until Max's new response replaces it." Visual behavior during interruption is defined.
- [ ] CHK024 — Are requirements for the session-end visual state specified (what does the CRT show after 50-turn/20K-token/30-min cap is hit)? [Coverage, Spec §FR-010] **⚠️ Minor gap.** The in-character sign-off is specified (FR-010 + bible §3.4 + Example 14). But what the CRT shows AFTER the sign-off isn't (does it go dark? show static? "End of broadcast" card?). **Recommendation:** Add a note: "After session cap sign-off, the CRT transitions to an in-character 'end of broadcast' state with the knob re-enabled for a new session."
- [x] CHK025 — Are requirements specified for the rate-limit breach visual state (in-character refusal copy presentation within CRT frame)? [Coverage, Spec §FR-020] **Addressed.** FR-020: "Breach triggers in-character refusal copy, not a system error." The refusal IS a Max response shown within the CRT frame using the same text display as any other response. No special visual state needed.

## Edge Case Coverage

- [x] CHK026 — Are visual requirements defined for extremely long single-word inputs or very short responses from Max (layout edge cases within the CRT frame)? [Edge Case, Gap] **Implementation detail.** CSS overflow/text-wrapping behavior within the CRT frame. Standard responsive text handling — word-break for long words, minimum content height for short responses. Not a spec-level concern.
- [x] CHK027 — Are requirements specified for viewport sizes between mobile and desktop breakpoints (tablet, small laptop)? [Edge Case, Gap] **Addressed by platform support.** Spec §Browser & Device Support lists supported browsers. Responsive design between mobile and desktop is standard web development practice. The spec's mobile constraints (reduced glitch intensity, press-and-hold mic) provide the breakpoint behavior. Tablet viewports interpolate between mobile and desktop — no additional spec needed.
- [x] CHK028 — Are requirements for the mic disclosure notice defined visually (first-activation modal/banner styling, persistent small-print placement relative to mic control)? [Edge Case, Spec §FR-019] **Addressed at constraint level.** FR-019: "explicit disclosure... naming the browser's speech provider, plus a persistent small-print notice at the mic control." The functional requirements (disclosure content, trigger, persistence) are specified. Visual styling is implementation detail.

## Non-Functional Requirements (Visual Performance)

- [x] CHK029 — Are CRT effect performance requirements defined per device class (desktop: 60fps specified in SC-004 — but is a mobile fps target defined)? [Gap, Spec §SC-004] **Addressed as sufficient.** SC-004 specifies 60fps desktop. Mobile: the spec says "reduced intensity" — the performance target is implicitly "smooth enough not to degrade the experience." A specific mobile fps target (e.g., 30fps) could be added, but the "reduced intensity" constraint already implies that mobile effects are scaled to device capability.
- [x] CHK030 — Are progressive enhancement tiers defined for devices that can run some but not all CRT effects? [Gap, Spec §Visual Presentation] **Addressed by design.** Two tiers are implied: desktop (full effects, 60fps) and mobile (reduced intensity). R3F + shaders provide natural progressive enhancement — the shader pipeline degrades gracefully based on GPU capability. A formal tier document isn't needed for a friends-and-family project; the implementation can detect WebGL capability and adjust.

## Accessibility

- [x] CHK031 — Are keyboard focus indicator visual requirements defined for all interactive elements (TV knob, text input, mic button, Forget me, Export)? [Completeness, Spec §Clarifications 2026-04-20] **Addressed.** Spec clarification 2026-04-20: "Keyboard navigation and visible focus indicators are required for MVP." The requirement exists. Visual design of focus indicators follows standard web accessibility (WCAG 2.1 §2.4.7 focus visible). No custom spec needed.
- [ ] CHK032 — Are color contrast requirements specified for text displayed within the CRT frame (Max's responses against the glitch/wireframe backdrop)? [Gap] **⚠️ Minor gap — accessibility concern.** Text within the CRT frame sits atop an animated wireframe/glitch backdrop. WCAG 2.1 contrast ratios (4.5:1 for normal text) should be ensured. **Recommendation:** Add a note to spec §Visual Presentation: "Text displayed within the CRT frame MUST maintain WCAG 2.1 AA contrast ratio (4.5:1) against the backdrop, using a semi-transparent background behind text or sufficient color separation."
- [ ] CHK033 — Are reduced-motion requirements specified for visitors who prefer reduced motion (prefers-reduced-motion media query handling for glitch effects)? [Gap] **⚠️ Gap — accessibility best practice.** Constant glitching, frame skips, and abrupt backdrop changes may affect visitors with motion sensitivities. The `prefers-reduced-motion` CSS media query should be respected. **Recommendation:** Add to spec §Visual Presentation or §Clarifications: "When the visitor's browser reports `prefers-reduced-motion: reduce`, glitch effects (frame skips, image tears, backdrop changes) MUST be minimized or disabled. Static scanlines and the wireframe backdrop at rest are acceptable."

## Notes

- Check items off as completed: `[x]`
- Add comments or findings inline
- Items are numbered sequentially for easy reference
- This checklist covers MVP requirements unless tagged [V1]

## Triage Summary (2026-04-25)

**Result: 28/33 addressed, 5 minor gaps. No blocking gaps.**

Many UX checklist items ask for exact visual parameters (dimensions, colors, animation timings) that the spec intentionally leaves to implementation. This is appropriate — the spec defines behavioral constraints and aesthetic direction, not pixel-perfect mockups.

### ⚠️ Minor Gaps (worth addressing, not blocking)

1. **CHK021 + CHK022 — TV off/on states.** The CRT's powered-off state and power-up transition animation aren't specified. These define the first visual impression. Recommend adding brief notes to spec §Visual Presentation.
2. **CHK024 — Session-end visual state.** What the CRT shows AFTER the sign-off (dark? static? "end of broadcast"?) isn't specified. Recommend adding a note.
3. **CHK032 — Color contrast.** Text within the CRT frame against the animated backdrop needs WCAG 2.1 AA contrast (4.5:1). Recommend adding an accessibility note.
4. **CHK033 — Reduced motion.** `prefers-reduced-motion` handling for glitch effects isn't specified. Recommend adding to spec §Visual Presentation or §Clarifications for accessibility compliance.
